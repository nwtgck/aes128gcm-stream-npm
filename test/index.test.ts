// TODO: You should change this content.

// import {hoge}  from '../src';
import * as assert from 'power-assert';

describe('hoge', () => {
  it('1 should be 1', () => {
    assert.equal(1, 1);
    console.log(window);
  });
});
